@model Team;

<h1 class="alert alert-warning text-center">@Model.Name Details</h1>

<div>
    <partial name="_teamData" />
</div>

<div>
    <a asp-action="EditView" asp-controller="Teams" class="btn btn-primary" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-controller="Teams" asp-action="IndexTeam" class="btn btn-secondary">Back to List</a>
</div>

